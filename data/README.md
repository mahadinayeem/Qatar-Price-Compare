# This directory holds the SQLite database (products.db)
# The database is created automatically when the scraper runs for the first time.
# It is committed to git so Vercel can read it at build time.
